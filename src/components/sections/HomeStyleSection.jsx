const SummarySection = ({ data, updateData }) => {
    return (
        <div>
            <label>
                HomeStyle
            </label>
        </div>
    );
};

export default SummarySection;
