const CustomerSection = ({ data, updateData }) => {
    return (
        <div>
            <label>
                HomeType
            </label>
        </div>
    );
};

export default CustomerSection;
